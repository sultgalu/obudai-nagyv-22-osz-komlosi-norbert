dependencies {
	implementation project(':warehouse-service')
}

application{
	mainClass = 'warehouse.app.SpringBootApp'
}

jar{
	manifest{
		attributes 'Main-Class': 'warehouse.app.SpringBootApp'
	}
	
	from{
		configurations.runtimeClasspath.collect {it.isDirectory() ? it: zipTree(it) }
	}
}